// 객체야 안녕?

// 빈 객체 user 생성
let user = {};

// user에 키가 name, 값이 John 인 프로퍼티 추가
user.name = "John";

// user에 키가 surname, 값이 Smith 인 프로퍼티 추가
user.surname = "Smith";

// name의 값을 Pete로 수정
user.name = "Pete";

// user에서 프로퍼티 name 삭제
delete user.name;